apiVersion: v1
kind: Service
metadata:
  name: rag-agent
  namespace: inference
  labels:
    app: rag-agent
spec:
  selector:
    app: rag-agent
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
